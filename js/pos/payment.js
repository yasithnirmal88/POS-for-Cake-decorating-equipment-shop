// Payment Processing Logic (Stub)
window.Payment = {
    // Will hold logic for processing Cash, Card, Split payments in a modal
    openModal: function(amount) {
        // To be implemented
        console.log("Opening payment modal for amount:", amount);
    }
};
